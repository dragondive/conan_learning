#include <iostream>
#include "sample.h"

/**
  * Used to verify that the project is configured correctly and runs without problems. :-)
  */
int main()
{
    std::cout << "main: " << get_hello() << " " << get_42() << std::endl;
}
